---
import { formatNum, formatDuration } from "../utils";
import aniversarios from "../data/aniversarios.json";
---

<section id="videos">
  <div class="container">
    <h2>🎂 Aniversarios 🎂</h2>

    <div>
      <div class="grid md:grid-cols-3 gap-4">
        {
          aniversarios.map((v) => (
            <a
              class="border-4 border-black bg-white p-2"
              href={`https://youtu.be/${v.id}`}
              target="_blank"
            >
              <img
                src={v.thumbnail}
                alt={v.title}
                class="w-full h-40 object-cover mb-2"
              />
              <p class="font-semibold text-sm line-clamp-2">{v.title}</p>
              <p class="text-xs mt-1">
                {new Date(v.publishedAt).getFullYear()}
              </p>
            </a>
          ))
        }
        <div class="border-4 border-black bg-white p-2">
          <img
            src="/img/10.webp"
            alt="Aniversario 2023"
            class="h-40 object-cover m-auto"
            style="filter: blur(1rem);"
          />
          <p class="font-semibold text-sm line-clamp-2">10 AÑOS</p>
          <p class="text-xs mt-1">2025</p>
        </div>
        <div
          class="border-4 border-black bg-white p-2"
          style="display: grid;align-content: center;justify-content: center;font-size: 2.5em;"
        >
          y muchos más...
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  #videos {
    padding-block: 3em;
    min-height: 100vh;
    display: grid;
    align-items: center;
  }
  #videos > .container > div {
    margin-top: 2em;
  }
</style>
